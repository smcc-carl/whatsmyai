---
layout: 'base.njk'
title: Your Best AI Tools Finder
---


<section style="background: url('./assets/bg1.gif'); background-size: cover;">
    <div class="bg-black/90 py-32">
    <p class="text-7xl font-bold text-center mb-6">Your Best AI Tools Finder</p>
    <form action="" class="px-[30%]">
        <input class="border border-white w-[100%] px-4 italic rounded-3xl py-2" type="text"  placeholder="Search the tools you need.">
    </form>

    <div class="mt-16 container mx-auto grid grid-cols-3 px-[10%]">
        <div class="text-center">
            <p class="text-5xl font-bold text-amber-300">100k</p>
            <p class="">Monthly Visitors</p>
        </div>
        <div class="text-center">
            <p class="text-5xl font-bold text-amber-300">5000+</p>
            <p class="">AI Tools Collections</p>
        </div>
        <div class="text-center">
            <p class="text-5xl font-bold text-amber-300">100%</p>
            <p class="">Free</p>
        </div>
    </div>
    </div>
</section>
<p class="text-center text-5xl font-semibold my-16">AI Tools For You</p>

<section class="">
    
    <div class="container mx-auto mt-16 bg-gray-800">
        <div class="flex justify-between">
        <p class="text-3xl font-medium">Featured</p>
        <a href="/collections/gpt" class="border text-amber-300 border-amber-300 rounded-full py-3 px-8 hover:bg-amber-300 hover:text-gray-800">View all collections</a>
        </div>
        <div class="container mx-auto mt-8">
            <ul class="grid grid-cols-6 gap-6 mx-auto">
                {% for featured in collections.featured  %}
                {%- if loop.index0 <6 -%}
                <a href="{{featured.url}}">
                    <li class="border border-gray-700 rounded-3xl p-4">
                        <img src="{{featured.data.image}}" alt="" class="w-18">
                        <p class="text-xl font-medium mt-4 text-amber-300">{{featured.data.title}}</p>
                        <p class="line-clamp-3 mt-4 text-gray-500">{{featured.data.description}}</p>
                        <p class="text-xl font-medium mt-4 text-green-300">{{featured.data.type}}</p>

                    </li>
                </a>
                {%- endif -%}
                {% endfor %}
            </ul>
        </div>
    </div>
</section>
<hr class="container mx-auto border-gray-700 border-.5 my-16">
<section class="">
    
    <div class="container mx-auto  bg-gray-800">
        
        <p class="text-3xl font-medium">Categories</p>
   
      
        <div class="container mx-auto mt-8">
            <ul class="grid grid-cols-5 gap-8 mx-auto">
                
              
                    <li class="border border-gray-700 rounded-3xl p-4">
                        <div class="mx-auto">
                            <p class="text-center text-2xl text-green-300 font-semibold pb-4">GPT Collections</p>
                            {% for gpt in collections.gpt %}
                                {%- if loop.index0 <6 -%}
                               
                        <div class=" gap-2 py-1 text-center">
                            <a href="{{gpt.url}}">
                        <div class="flex items-center gap-4">
                        <img src="{{gpt.data.image}}" alt="" class="w-8">
                        <p class=" text-md  hover:text-green-300">{{gpt.data.title}}</p>
                        </div>
                        </a>
                    </div>

                       
                        {%- endif -%}
                        {% endfor %}
                        <div class="w-full text-center mt-10">
                        <a href="#" class="rounded-full px-8 py-3 bg-green-300 text-gray-700">View all Collections</a>
                        </div>
                        </div>

                    </li>
                    <li class="border border-gray-700 rounded-3xl p-4">
                        <div class="mx-auto">
                            <p class="text-center text-2xl text-green-300 font-semibold pb-4">Web Development</p>
                            {% for assistant in collections.assistant %}
                                {%- if loop.index0 <6 -%}
                               
                        <a href="{{gpt.url}}">
                        <div class="flex gap-2 py-2 text-center">
                        <img src="{{gpt.data.image}}" alt="" class="w-10">
                        <p class="  mt-4 hover:text-green-300">{{gpt.data.title}}</p>
                        </div>
                        </a>

                       
                        {%- endif -%}
                        {% endfor %}
                        <div class="w-full text-center mt-10">
                        <a href="#" class="rounded-full px-8 py-3 bg-green-300 text-gray-700">View all Collections</a>
                        </div>
                        </div>

                    </li>
            
               
            </ul>
        </div>
    </div>
</section>

